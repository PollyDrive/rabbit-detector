export const CANVAS_WIDTH = 2752;
export const CANVAS_HEIGHT = 1536;

// Smallest desktop viewport the layout scales down to (~12" laptop).
// Below this, the canvas stops shrinking further and the page scrolls.
export const MIN_DESKTOP_WIDTH = 1280;
